/** Generic family profile (it. 33) — fallback for DeepSeek/GLM/Kimi/OpenAI: no
 *  specific notes emerged so far, empty text (no section added to the prompt). */
export const DEFAULT_PROFILE = {
  id: "default",
  text: "",
};
